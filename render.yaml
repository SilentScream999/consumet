services:
  - type: web
    name: consumet-api # Name of the service
    env: docker
    repo: https://github.com/SilentScream999/consumet
    plan: free
    branch: main
    envVars:
      - key: PORT
        value: 3000
